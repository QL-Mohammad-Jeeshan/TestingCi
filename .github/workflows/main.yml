name: CI-CD (Demo)

on: push

jobs:
  build_project:
    name: Build
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Choose Xcode version
	      run: echo "Working"
